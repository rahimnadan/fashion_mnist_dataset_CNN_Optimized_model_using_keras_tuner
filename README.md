# fashion_mnist_dataset_CNN_Optimized_model_using_keras_tuner

In this project a CNN model is trained on fashion MNIST dataset. The Model is optimized with Hyperparameter tunning using keras_tuner. It gives 99% accuracy when optimized.
